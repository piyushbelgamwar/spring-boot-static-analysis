<build>
    <plugins>
        <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-checkstyle-plugin</artifactId>
            <version>3.3.1</version>
            <dependencies>
                <!-- Add your checkstyle library as a dependency to the plugin -->
                <dependency>
                    <groupId>com.security</groupId>
                    <artifactId>quality-check-library</artifactId>
                    <version>1.0.0</version>
                </dependency>
                <!-- This ensures the plugin uses the same version of checkstyle -->
                <dependency>
                    <groupId>com.puppycrawl.tools</groupId>
                    <artifactId>checkstyle</artifactId>
                    <version>10.12.5</version>
                </dependency>
            </dependencies>
            <configuration>
                <!-- Reference the checkstyle.xml from your quality-check-library -->
                <configLocation>com/security/checkstyle.xml</configLocation>
                <encoding>UTF-8</encoding>
                <consoleOutput>true</consoleOutput>
                <failsOnError>true</failsOnError>
                <linkXRef>false</linkXRef>
            </configuration>
            <executions>
                <execution>
                    <id>validate</id>
                    <phase>verify</phase>
                    <goals>
                        <goal>check</goal>
                    </goals>
                </execution>
            </executions>
        </plugin>
    </plugins>
</build>
