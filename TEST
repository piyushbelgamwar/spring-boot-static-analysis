// Original code in performAsyncSchemaValidation
StreamSource source = new StreamSource(inputStream);

// Should be replaced with:
String xmlContentString = IOUtils.toString(inputStream, StandardCharsets.UTF_8);
Reader xmlContent = new StringReader(xmlContentString);
StreamSource source = new StreamSource(xmlContent);
